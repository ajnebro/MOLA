"""Per-framework adapters: own sampling and evaluation, emit the interchange Sample.

The core never imports from here and never sees a problem object (see CLAUDE.md, "Design
principle: framework-independent via an interchange contract").
"""
